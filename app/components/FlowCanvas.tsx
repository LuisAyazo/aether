import { Node } from 'reactflow';
import { NodeGroupData } from './nodes/NodeGroup';

export const FlowCanvas = () => {
  const handleContextMenu = (event: React.MouseEvent, node?: Node<NodeGroupData>) => {
    // ... existing code ...
  };
}; 