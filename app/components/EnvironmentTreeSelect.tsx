import { Node } from 'reactflow';
import { NodeGroupData } from './nodes/NodeGroup';

export const EnvironmentTreeSelect = () => {
  const handleNodeSelect = (node: Node<NodeGroupData>) => {
    // ... existing code ...
  };
}; 