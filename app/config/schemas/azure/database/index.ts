// Azure Database Schemas
// TODO: Define and export actual database resource schemas

export const azureDatabaseResources = {
  // Ejemplo:
  // sqlServer: {
  //   schema: async () => (await import('./sqlserver/sqlServerSchema')).sqlServerSchema,
  //   fields: async () => (await import('./sqlserver/sqlServerFields')).sqlServerFields,
  //   defaults: async () => (await import('./sqlserver/sqlServerDefaults')).sqlServerDefaults,
  //   templates: async () => (await import('./sqlserver/sqlServerTemplates')).sqlServerTemplates,
  // },
  // cosmosDb: { ... },
} as const;
