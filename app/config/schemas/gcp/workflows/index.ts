import gcpWorkflowsWorkflowResource from './workflow';

// Agrupa todos los recursos de Workflows en un solo objeto para facilitar su importación.
const gcpWorkflowsResources = {
  workflow: gcpWorkflowsWorkflowResource,
  // Aquí se podrían añadir otros recursos de Workflows en el futuro si es necesario.
};

export default gcpWorkflowsResources;
